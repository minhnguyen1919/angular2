div
  form.col-md-4([formGroup]="addUserForm", (ngSubmit)="onSubmit(addUserForm.value)")
    div.form-group
      label(for="userName") Username
      input#userName.form-control(type="text", [formControl]="addUserForm.controls['username']")
      div(*ngIf="addUserForm.controls['username'].hasError('required')", class="alert alert-danger")
        | Name is required
      div(*ngIf="addUserForm.controls['username'].hasError('minlength')", class="alert alert-danger")
        | Name is invalid
      div(*ngIf="addUserForm.controls['username'].hasError('pattern')", class="alert alert-danger")
        | Name is only character

    div.form-group
      label(for="userAddress") Address
      input#userAddress.form-control(type="text", [formControl]="addUserForm.controls['address']")

    div.form-group
      label(for="userAge") Age
      input#userAge.form-control(type="text", [formControl]="addUserForm.controls['age']")
      div(*ngIf="addUserForm.controls['age'].hasError('invalidAge')", class="alert alert-danger")
        | Age is invalid

    div.form-group
      label(for="userEmail") Email
      input#userEmail.form-control(type="text", [formControl]="addUserForm.controls['email']")
      div(*ngIf="addUserForm.controls['email'].hasError('required')", class="alert alert-danger")
        | Email is required
      div(*ngIf="addUserForm.controls['email'].hasError('invalidEmail')", class="alert alert-danger")
        | Email is invalid

    button.btn.btn-default(type="submit") Add user
    button.btn.btn-default(type="button") Cancel
indicator(*ngIf="isLoading")
